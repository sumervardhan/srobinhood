{
  "buildCommand": "prisma migrate deploy && prisma generate && next build",
  "crons": [
    {
      "path": "/api/worker/prices",
      "schedule": "* * * * *"
    }
  ],
  "functions": {
    "src/app/api/worker/prices/route.ts": {
      "maxDuration": 300
    }
  }
}
